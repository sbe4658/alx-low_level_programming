#ifndef FP_H
#define FP_H

/* Function's section */
void print_name(char *, void (*)(char *));
void print_name_uppercase(char *name);
void print_name_as_is(char *name);

/* Header's section */
#include <stdio.h>
#include <stdlib.h>

#endif
