#ifndef MAIN_H
#define MAIN_H

/* Header's section */
#include <stdio.h>

/* Function's section */
unsigned int binary_to_uint(const char *);
int _putchar(char c);
void print_binary(unsigned long int n);

#endif
