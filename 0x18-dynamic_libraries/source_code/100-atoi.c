#include "main.h"
/**
 * _atoi - convert a string to an integer.
 * @s: given string.
 *
 * Return: the converted string.
 */
int _atoi(char *s)
{
	s++;
	printf("In progress...\n%s\n", s);
	return (0);
}
